import React from 'react';
import useTitle from '../hooks/useTitle';

const Order_review = () => {
      useTitle('Orders-review')
      return (
            <div>
                  Check out
            </div>
      );
};

export default Order_review;