import React from 'react';
import useTitle from '../hooks/useTitle';

const Errorpage = () => {
      useTitle('Error')
      return (
            <div>
                  error page
            </div>
      );
};

export default Errorpage;